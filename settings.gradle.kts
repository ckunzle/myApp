pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = "myApp"

enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")
